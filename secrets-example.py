secrets = {
    # Note, MQTT TLS is NOT yet support. Maybe some day.
    'broker': 'the.mqtt.hostname', # MQTT Broker hostname or IP.
    'port': 1883,  # MQTT Broker port. Will default to 1883 if not listed.
    'username': 'a username',  # MQTT username to log in as.
    'password': 'a better password than this'  # MQTT password for logins.
}